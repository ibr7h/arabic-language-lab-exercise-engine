# تقرير التحقق — الإصدار v3

تاريخ التحقق: 2026-09-16

## ما تم اختباره

- فحص syntax لجميع ملفات JavaScript الأساسية وService Worker.
- اختبارات محرك العربية (`arabic-engine.test.mjs`) — ناجحة.
- اختبارات وظائف السبورة v3 (`board-v3.test.mjs`) — ناجحة.
- فحص عدم وجود HTML IDs مكررة — ناجح.
- فحص المسارات المحلية وملفات PWA وmanifest وملفات cache — ناجح.
- اختبار تفاعلي داخل Chromium عبر صفحة in-memory لتجاوز حظر localhost في بيئة الاختبار:
  - وجود أكثر من كلمة مكتملة على السبورة — ناجح.
  - التبديل بين الترتيب الأبجدي وترتيب كتاب لغتي — ناجح.
  - تحديد الكل — ناجح.
  - استبدال الحركة دون تراكم الحركات — ناجح.
  - اجتماع الشدة مع الحركة — مغطى باختبار الوحدة.
  - الحفاظ على الألف العادية عند توليد الأشكال السياقية — ناجح.
  - قلم السبورة: رسم Stroke، ثم Undo — ناجح.

## ملاحظة بيئة الاختبار

التصفح المباشر إلى localhost/file:// محظور بسياسة البيئة (`ERR_BLOCKED_BY_ADMINISTRATOR`)، لذلك تم تشغيل اختبار DOM والتفاعل عن طريق حقن الصفحة والأصول محليًا داخل Chromium. لا يغني ذلك عن تجربة نهائية على Safari في iPhone بعد النشر على HTTPS/GitHub Pages.
