# معمل اللغات العربية التفاعلي — PWA

نسخة معاد تنظيمها من النموذج الأحادي، مع إصلاحات المرحلة الأولى.

## ما تم إنجازه
- إصلاح تجزئة الحرف العربي مع حركاته باستخدام `Intl.Segmenter` مع fallback.
- إصلاح قاعدة السكون والمد في التحليل الديناميكي.
- القراءة الهرمية الصوتية أصبحت تعتمد نهاية النطق بدل مؤقتات ثابتة.
- حفظ رسم السبورة عند تغيير الحجم.
- السماح بتكبير الصفحة على الهاتف.
- فصل CSS وJavaScript عن HTML.
- إزالة الاعتماد على Tailwind CDN وTone.js وCanvas Confetti.
- Tailwind CSS مبني محلياً.
- Web Audio محلي للمؤثرات الصوتية، وConfetti محلي خفيف.
- Manifest + Service Worker + أيقونات PWA.
- اختبار تلقائي لمحرك العربية.

## التشغيل محلياً
شغّل خادماً محلياً داخل مجلد المشروع، مثلاً:

```bash
python3 -m http.server 8080
```
ثم افتح `http://localhost:8080`.

> ملاحظة: Google Fonts بقيت تحسيناً اختيارياً. إذا لم يتوفر الإنترنت يستخدم التطبيق خطوط fallback ولا تتعطل الوظائف الأساسية.

## الاختبار
```bash
node tests/arabic-engine.test.mjs
node --check assets/js/app.js
node --check sw.js
```

## GitHub Pages
ارفع محتويات هذا المجلد إلى جذر المستودع وفعّل GitHub Pages من الفرع `main`. المسارات كلها نسبية لتعمل داخل مسار المستودع.


## إضافات v3
- اختيار ترتيب الحروف بين **كتاب لغتي** و**الترتيب الهجائي**.
- السبورة المغناطيسية تدعم عدة كلمات مكتملة في الوقت نفسه.
- تحديد الكل وتحريك/حذف المجموعة المحددة.
- الحركات قابلة للتبديل والإزالة مع تموضع بصري مستقل.
- قلم السبورة يستخدم Pointer Events وتنعيم منحنيات ودقة شاشة مرتفعة.

## v4 additions
- اضغط على أي مكان فارغ في السبورة المغناطيسية لإلغاء التحديد.
- الضغط على حرف من كلمة مكتملة يحدد الكلمة كاملة لتحريكها أو تكبيرها/تصغيرها كمجموعة، مع إبقاء الحرف المضغوط نشطًا لتغيير حركته.
- دعم صريح لأشكال الألف: **ا، أ، إ، آ**.
- دبدوب التحليل يحافظ على هيئة الحرف حسب موقعه الحقيقي في الكلمة.
- تحسين قلم السبورة للجوال والتابلت.
- تكبير واضح للتشكيل على السبورة.
- تكبير وتصغير الحروف المحددة من 65% إلى 180%.

## الحقوق
Copyright © 2026 Ibrahim Alneami — All Rights Reserved.
راجع `LICENSE.txt` لشروط الملكية وإعادة البيع والترخيص التجاري.


## التقوية الأمنية في v4 Secure

هذه النسخة تعمل دون خطوط أو سكربتات خارجية، وتستخدم CSP مع `script-src 'self'` ومعالجات أحداث خارجية آمنة. راجع `SECURITY_REVIEW_v4.md`.


## v4.1 — تحريك حرف واحد داخل الكلمة

- الضغطة الأولى على حرف من كلمة مكتملة تحدد الكلمة كاملة.
- الضغطة الثانية على حرف من الكلمة المحددة تختار الحرف وحده.
- الضغط المطوّل على الهاتف/اللوحي يحدد الحرف مباشرة.
- زر **فك الكلمة** يحولها إلى حروف مستقلة، وزر **تجميع** يعيد ربط الحروف.
- تمييز بصري: بنفسجي للكلمة، برتقالي للحرف، وأخضر مزرق للتحديد المتعدد.

---

## Exercise Engine Prototype — نسخة مستقلة للمراجعة

هذه النسخة **لا تستبدل v4 أو v4.1**. أنشئت كفرع تطوير مستقل لاختبار منطق الأنشطة قبل تحويله إلى نسخ المنصات.

### أوضاع السبورة الجديدة

- **وضع حر:** كل حرف مستقل تمامًا، مناسب للنثر والتركيب اليدوي.
- **كوّن كلمة:** يحدد المعلم كلمة هدف، ثم ينثر المحرك حروفها كقطع مستقلة غير مرتبطة مسبقًا، ويعرض خانات ترتيب من اليمين إلى اليسار.
- **كلمات مكتملة:** يحافظ على سلوك الكلمات الجاهزة كمجموعات مع الفك وإعادة التجميع.

### نشاط «كوّن كلمة»

- كلمة هدف يكتبها المعلم أو يختارها من أمثلة جاهزة.
- خيار إظهار/إخفاء الحركات.
- خيار عرض الحرف منفصلًا أو بشكله السياقي في الكلمة.
- خيار إظهار/إخفاء الكلمة الهدف.
- نثر مستقل للحروف، ثم سحبها إلى خانات RTL.
- تبديل الحروف بين الخانات وإخراجها وإعادة ترتيبها.
- تحقق يدوي أو تلقائي، مع عدد المواضع الصحيحة وتلميح للخانة التالية.

### بنية المنصات

النواة مشتركة، مع ملفات تعريف مستقلة تحت `profiles/` لـ:

- `ios.json` — iOS PWA
- `android.json` — Android PWA
- `webos.json` — webOS TV PWA

التفاصيل في `docs/ARCHITECTURE.md`.

### اختبارات المحرك

```bash
node tests/exercise-engine.test.mjs
node tests/exercise-board-piece.test.mjs
```
