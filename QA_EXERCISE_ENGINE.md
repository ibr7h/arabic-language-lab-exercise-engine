# QA — Exercise Engine Prototype v1

التاريخ: 2026-09-17

## اختبارات الرجوع Regression

نجحت:
- `tests/arabic-engine.test.mjs`
- `tests/board-v3.test.mjs`
- `tests/board-v4.test.mjs`
- `tests/board-v41.test.mjs`

وهذا يعني أن منطق العربية، أشكال الألف، دبدوب التحليل، القلم باللمس، وتحديد حرف واحد داخل كلمة لم تتأثر بالتفرع الجديد.

## اختبارات المحرك الجديد

نجحت:
- `tests/exercise-engine.test.mjs`
- `tests/exercise-board-piece.test.mjs`

تم التحقق من:
- استخراج وحدات `كِتَابٌ` كـ `كِ / تَ / ا / بٌ`.
- المقارنة بالحركات وبدون الحركات.
- النثر لا يعيد الترتيب الأصلي عمدًا إذا كانت الكلمة متعددة الحروف.
- قطع نشاط «كوّن كلمة» لا تحمل `wordId`؛ أي أنها مستقلة فعليًا.
- اكتشاف iOS / Android / webOS من ملف المنصة.

## QA تفاعلي على Chromium بحجم جوال 390×844

نجح السيناريو التالي دون أخطاء JavaScript:
1. اختيار «كوّن كلمة».
2. إدخال `كِتَابٌ`.
3. إنشاء 4 حروف مستقلة منثورة و4 خانات RTL.
4. سحب كل حرف إلى خانته.
5. التحقق: ظهرت رسالة نجاح.
6. سحب الحرف الأول إلى خانة الحرف الثاني: تم تبديل الحرفين بين الخانتين بدل منع الحركة.
7. التحقق: النظام أبلغ أن 2 من 4 في الموضع الصحيح.
8. إعادة النثر: عادت جميع القطع خارج الخانات.

## فحوص Static/PWA

- لا توجد IDs مكررة.
- لا توجد أصول محلية مفقودة في HTML أو Service Worker.
- Manifest وملفات profiles JSON سليمة.
- لا توجد `onclick=` مباشرة؛ ما زال dispatcher التصريحي الآمن مستخدمًا.
- لا توجد روابط HTTP/HTTPS خارجية في الصفحة.

## ملاحظة

هذا Prototype للمراجعة، وليس استبدالًا لـ v4/v4.1 المنشورة.
